generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Student {
  id        String   @id @default(cuid())
  name      String
  instrument String
  grade     String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  reports   Report[]
}

model Report {
  id          String   @id @default(cuid())
  month       String   // e.g. "April-Mei"
  year        Int
  paymentDate DateTime?
  studentId   String
  student     Student  @relation(fields: [studentId], references: [id])
  entries     ReportEntry[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ReportEntry {
  id              String   @id @default(cuid())
  date            DateTime
  material        String
  isTeacherSigned Boolean  @default(false)
  isStudentSigned Boolean  @default(false)
  reportId        String
  report          Report   @relation(fields: [reportId], references: [id])
}
